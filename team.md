# Team Contributions & Project Handover (ส่งต่องาน)

เอกสารนี้ใช้สำหรับการบันทึกประวัติการทำงานของทีมและรายละเอียดโครงสร้างที่ได้ถูกพัฒนาเอาไว้ เพื่อส่งมอบให้ผู้รับผิดชอบงานหรือนักพัฒนาคนต่อไปเข้ามาทำงานต่อได้อย่างรวดเร็ว

---

## 👨‍💻 สิ่งที่พัฒนาเสร็จเรียบร้อยแล้ว

**ผู้ทำ/รับผิดชอบ:** `thanawat-pete`

คุณ thanawat-pete ได้ทำการสร้างและวางรากฐานโครงสร้าง (Project Architecture) แบบ Full-Stack ของเว็บไซต์ **"จอมบึง มาราธอน 2026"** อย่างรอบด้าน ดังนี้:

### 1. สร้างหน้าเว็บ (Frontend)

- เคาะโครงสร้าง **`index.php`** และเขียนแท็ก Semantic HTML องค์ประกอบหลักๆ เช่น แถบนำทาง (Navbar), หน้าโฮมเพจดึงดูดสายตา (Hero Section), ข้อมูลระยะวิ่ง 3 ระดับ และส่วนของ Footer
- ออกแบบและเขียนไฟล์ **`style.css`** แบบ Vanilla CSS ล้วนๆ ดึงฟอนต์ Prompt เพื่อให้รองรับภาษาไทยอย่างสวยงาม จัดวาง Layout แบบทันสมัย ดูพรีเมียมด้วยโทนส้ม-น้ำเงินเข้ม พร้อมการแสดงผลแบบ Responsive แหล่มๆ

### 2. วางโครงสร้างระบบหลังบ้าน (Backend - Node.js Express)

- สร้างไฟล์ **`package.json`** และจัดการ dependencies ตัวสำคัญเสร็จสิ้น (`express`, `cors`, `dotenv`) พร้อมสคริปต์รันและรีโหลดอัตโนมัติ
- สร้างและแยกสัดส่วนโค้ด Backend อย่างมีมาตรฐานในโฟลเดอร์ **`src/`** ดังนี้:
  - `src/index.js`: ไฟล์ Entry point รันเซิร์ฟเวอร์ด้วย Express ทำงานบนพอร์ต `5000`
  - `src/routes/api.js`: กำหนดเส้นทาง URL เอาไว้ให้แล้ว (เช่น รับสมัครนักวิ่ง, ดึงสถิติของแอดมิน)
  - `src/controllers/marathonController.js`: ประมวลผลและเตรียม Response ค่าต่างๆ
  - `src/services/marathonService.js`: เขียนโครงสร้าง Logic ภายในไว้ให้ และจำลองข้อมูลตัวแปรชั่วคราว (Mock Registration) ขึ้นมาก่อน
  - `src/middleware/authMiddleware.js`: ซ้อมเขียนมิดเดิลแวร์สำหรับการตรวจสอบสิทธิ์ Admin เผื่อใช้กับหน้าหลังบ้าน
  - `src/config/db.js`: โครงสร้างไฟล์สำหรับทำการเชื่อมต่อฐานข้อมูลในอนาคต
- สร้างไฟล์ **`.env`** พร้อมระบุตัวแปรหลัก (รหัสผ่านฐานข้อมูล / เลขพอร์ต)
- สร้าง **`.gitignore`** และ **`README.md`** พิมพ์อธิบายวิธีการเซ็ตอัพและการรันเซิร์ฟเวอร์ไว้ให้ทีมอ่านเข้าใจได้ง่าย

---

## 🎯 สิ่งที่คนต่อไปจะต้องทำเพิ่ม (Next Steps)

เพื่อให้เว็บไซต์เปิดรับสมัครได้จริง นี่คืองานที่ถูกเผื่อไว้ให้คนต่อไปมาลุย:

1. **ต่อฐานข้อมูลจริง (Database Connection):** เปลี่ยนข้อมูลจำลองใน `services/marathonService.js` ไปเป็นการเชื่อมต่อกับฐานข้อมูลจริงๆ (เช่น MySQL/MariaDB) ผ่านหน้า `config/db.js`
2. **ทำฟอร์มรับสมัครในหน้า PHP:** ในไฟล์หน้าเพจ `index.php` ให้สร้าง `<form>` ข้อมูลผู้สมัคร และเขียนสคริปต์ AJAX/Fetch เข้ามายิงข้อมูลใส่ API ฝั่ง Node.js
3. **ออกแบบหน้าระบบแอดมิน:** สร้างระบบล็อกอินของแอดมิน เพื่อที่จะดึงข้อมูลนักวิ่งที่ลงทะเบียนสำเร็จออกมาจากระบบได้อย่างปลอดภัย
